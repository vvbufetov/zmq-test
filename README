Version 4.

    Both the client and the server applicatiion use template classes.
    Template parameters define classes for the message and the filter.

    sender      - reads STDIN then sends line to subscribers.
    client      - subscribes on a substring matching then prints out
                  text lines has been received from the sender.
